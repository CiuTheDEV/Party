name = "implementer"
description = "Agent for implementing changes in the repository."
model = "gpt-5.4-mini"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
developer_instructions = """
Make changes carefully and minimally.
Do not do broad refactors without a clear need.
Prefer small, safe patches that fit the project structure.
"""
