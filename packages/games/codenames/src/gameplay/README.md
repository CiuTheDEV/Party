# Codenames gameplay

This folder is reserved for gameplay flow that is specific to Codenames.

Suggested split:
- `host/` for host-side game control
- `player/` for player-facing turn logic
- `shared/` for common gameplay models and helpers

Shared menu and shared setup chrome should stay outside this folder.
